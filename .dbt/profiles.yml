eth:
  outputs:
    ci:
      account: MMHQXVL-ST20969
      database: ci
      private_key_path: "{{ env_var('DBT_PRIVATE_KEY_PATH') }}"
      role: ACCOUNTADMIN
      schema: CI_SCHEMA
      threads: 1
      type: snowflake
      user: ttteety
      warehouse: compute_wh
  target: ci
